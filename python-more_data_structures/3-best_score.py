def best_score(a_dictionary):
    if not  a_dictionary:
        return None
    
    best_key = value
    best_key = best_score
    best_score = int

    for key, value in a_dictionary:
        if value > best_score:
            return best_key

    
