def best_score(a_dictionary):
    if not  a_dictionary:
        return None
    
    key = value
    best_score = int

    for key, value in a_dictionary:
        if value > best_score:
            return key

    
